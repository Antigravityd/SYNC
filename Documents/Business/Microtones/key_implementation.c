// Current MCU choice: AT90CAN64.

uint16_t key_id = 0;
uint16_t count = 0;

int main() {
  // initialize SPI peripheral
  // register an initial interrupt handler. This should:
  //     1. interpret the first 16 (?) bits over the bus as an ID assignment
  //     2.



}
